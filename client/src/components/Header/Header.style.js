import {makeStyles} from '@material-ui/core/styles';

const useStyles = makeStyles({
    header: {
        textAlign: 'center',
        fontSize: '22px',
        background: '#fa9e6a',
        borderRadius: 10,
        color: 'white',
        marginInline: '12px',
    },
});

export { useStyles as Styles };